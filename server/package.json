{
  "name": "server",
  "version": "1.0.0",
  "description": "Servidor para subir archivos a la página web de criptografía.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./dist && tsc",
    "dev": "tsnd --respawn --clear src/app.ts",
    "start": "npm run build && node dist/app.js"
  },
  "author": "Serge Eduardo Martínez Ramírez",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.5",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@types/express-fileupload": "^1.5.1",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-fileupload": "^1.5.1"
  }
}
